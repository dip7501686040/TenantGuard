# TenantGuard project root .npmrc
# Use pnpm as default package manager
package-manager=pnpm@latest
# Use public npm registry (you can replace if you have a private one)
registry=https://registry.npmjs.org/
# Ensure consistent lockfile handling
lockfile=true
# Keep dependency versions exact for reproducible installs
save-exact=true
